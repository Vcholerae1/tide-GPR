# Module: tide.common (deprecated)

`tide.common` is a compatibility shim that re-exports symbols from:
- `tide.callbacks`
- `tide.resampling`
- `tide.cfl`
- `tide.validation`
- `tide.padding`

Prefer importing from the dedicated modules above for clearer ownership.
